import 'package:flutter/material.dart';

class FtcScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FTC'),
      ),
      body: Center(
        child: Text('FTC Page'),
      ),
    );
  }
}
