import 'package:flutter/material.dart';

class FllScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fll'),
      ),
      body: Center(
        child: Text('Fll Page'),
      ),
    );
  }
}
